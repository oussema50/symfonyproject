<form method="post" action="{{ path('app_dining_table_delete', {'id': dining_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dining_table.id) }}">
    <button class="btn">Delete</button>
</form>
