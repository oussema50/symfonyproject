{% extends 'base.html.twig' %}

{% block title %}DiningTable{% endblock %}

{% block body %}
    <h1>DiningTable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dining_table.id }}</td>
            </tr>
            <tr>
                <th>TableNumber</th>
                <td>{{ dining_table.tableNumber }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ dining_table.capacity }}</td>
            </tr>
            <tr>
                <th>Isreserved</th>
                <td>{{ dining_table.isreserved ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dining_table_index') }}">back to list</a>

    <a href="{{ path('app_dining_table_edit', {'id': dining_table.id}) }}">edit</a>

    {{ include('dining_table/_delete_form.html.twig') }}
{% endblock %}
