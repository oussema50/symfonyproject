{% extends 'base.html.twig' %}

{% block title %}Restau{% endblock %}

{% block body %}
    <h1>Restau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restau.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ restau.name }}</td>
            </tr>
            <tr>
                <th>Log</th>
                <td>{{ restau.log }}</td>
            </tr>
            <tr>
                <th>Lat</th>
                <td>{{ restau.lat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_restau_index') }}">back to list</a>

    <a href="{{ path('app_restau_edit', {'id': restau.id}) }}">edit</a>

    {{ include('restau/_delete_form.html.twig') }}
{% endblock %}
